//Q.7: Write a program to make a such pattern like a right angle triangle with a number which will repeat a number in- .
// a row . the pattern like : 1 , 22 , 33 , 4444

void main() {
  int rows = 4; // Number of rows for the pattern

  for (int i = 1; i <= rows; i++) {
    for (int j = 1; j <= i; j++) {
      // Print the repeating number based on the row number
      print('$i');
    }
    print(''); // Move to the next line after each row
  }
}